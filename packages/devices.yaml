group:
  Devices_view:
    name: Devices
    view: no
    icon: mdi:cellphone-link
    entities:
      - group.iPhone
      - group.H2G2
      - group.iCover
      - group.Dimmers_status_card
      - group.gt86

  iPhone:
    name: iPhone
    view: no
    entities:
      - device_tracker.milo
      - sensor.milo_battery_level
      - sensor.milo_battery_state

  H2G2:
    name: H2G2
    view: no
    entities:
      - device_tracker.h2g2
      - sensor.h2g2_battery_level
      - sensor.h2g2_battery_state

  iCover:
    name: iCover
    view: no
    entities:
      - sensor.icover_battery_level
      - sensor.icover_battery_state

  GT86:
    name: GT86
    view: no
    entities:
      - device_tracker.gt86_gt86

  #cnf:
  #  view: no
  #  icon: mdi:account
  #  entities:
  #  - group.iPhone

