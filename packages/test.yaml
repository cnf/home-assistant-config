script:
  lr_message_flash:
    alias: Message - Flash
    sequence:
      - service: light.turn_on
        data:
          entity_id: light.lr_message
          flash: long
          brightness_pct: 100
          color_name: red
